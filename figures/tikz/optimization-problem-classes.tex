% Classes of optimization problems, and the modeling paradigms that cut across
% them.
%
% Re-authored to replace media/classification.png, which was a verbatim SCAN of
% Biegler (2010), Figure 1.1 ("Classes of optimization problems"), caption and
% all -- not searchable, not scalable, not greyscale-checkable, and not ours.
%
% It is an ADAPTATION, not a redrawing. Four things are different, each asked
% for by Prof. Dowling (2026-08-25):
%
%   1. COVERAGE IS COLOUR-CODED. "I want to color-code the problem classes we
%      will not discuss this semester."
%   2. THE CONVEX BRANCH IS A NESTING, NOT A FAN. "Where does conic
%      optimization fit into this diagram?" Biegler's flat Convex -> {LP, QP}
%      cannot express LP < QP < SOCP < SDP, so the four are drawn as a chain
%      of inclusions instead of as siblings.
%   3. THE MIXED-INTEGER BRANCH IS ALSO A NESTING, and MIQP is in it. "What
%      about MIQPs and other specific problem types?"
%
% ---------------------------------------------------------------------------
% BOTH CHAINS READ GENERAL -> SPECIFIC DOWNWARD, and that is a coherence
% constraint rather than a taste. Prof. Dowling, 2026-08-25: "I am a little
% curious that your ordering goes from most specific (top) to most general
% (bottom). I think it should be reversed, with the most general at the top."
% He is right: the TREE already narrows downward (Optimization problem ->
% Continuous/Discrete -> Differentiable/Nondifferentiable -> Convex/Nonconvex),
% so chains running the other way contradict the thing they hang from.
%
% 🔴 THE RELATION SYMBOL IS A MATHEMATICAL CLAIM AND MUST BE FLIPPED WITH THE
% BOXES. Reversing SDP/SOCP/QP/LP while leaving "c" in place would assert
% SDP c SOCP c QP c LP, which is FALSE. The arrows keep pointing down, with the
% whole rest of the figure, and the symbol becomes "\supseteq": read downward,
% SDP >= SOCP >= QP >= LP and MINLP >= MIQP >= MILP >= IP, both true. Turning
% the arrows upward instead would have kept "\subseteq" but given the picture
% two reading directions, which is the defect being fixed. The margin
% annotations say "more specialized ->" downward to match.
%   4. THERE IS A PARADIGM BAND AT THE BOTTOM. "I also want to have boxes at
%      the bottom that highlight modeling paradigms, such as stochastic
%      programming and dynamic optimization."
%
% ---------------------------------------------------------------------------
% THE PEDAGOGICAL POINT OF THE BAND: the paradigms are ORTHOGONAL to the tree,
% not another branch of it. A stochastic program can be an LP or an MINLP; a
% dynamic optimization problem discretizes INTO an NLP. So the band is drawn
% as a different KIND of object -- rounded tags inside a dashed enclosure, no
% tree edges, one arrow pointing back up at the whole tree rather than at any
% node in it. If it were drawn as more tree nodes it would teach the opposite
% of what it is for. The five entries are the course's own Part I list
% (see ../../optimization-private/lecture-notes/lectures/introduction.tex,
% "The semester").
%
% ---------------------------------------------------------------------------
% GREYSCALE. The colour policy (figures/dowling.mplstyle header, and
% figures/README.md "Course additions") is: a distinction may be carried by
% colour, but NEVER by colour alone. Coverage therefore has THREE redundant
% encodings, two of which are colour-free:
%
%     covered            white fill      SOLID   rule    black text
%     modeled-only       blue tint       DASHED  rule    black text
%     not covered        grey tint       DOTTED  rule    grey text
%
% The two tints are deliberately close in luminance -- they are tints, so they
% must be, to keep the text on them readable. The dash pattern is what a
% photocopy reads. The legend strip repeats all three treatments, so a reader
% in black and white decodes the figure from the legend alone.
%
% ---------------------------------------------------------------------------
% WIDTH IS A LEGIBILITY CONSTRAINT, NOT A LAYOUT PREFERENCE. The website gives
% a figure ~720 CSS px of column width whatever its natural size, so the
% authored width in cm fixes how big the type is on screen: at 24 cm wide,
% \footnotesize renders about 5 px tall and nobody can read it. This picture is
% therefore laid out TALL AND NARROW (~14.5 cm), with both inclusion chains
% running vertically and the paradigm band on two rows. Height is free --- only
% width is scaled. Measured in the built page before and after.
%
% ---------------------------------------------------------------------------
% NO \ref/\eqref anywhere: this file is rendered standalone by figures/Makefile
% as well as \input by the handout, and a cross-reference would print "??".

% Okabe-Ito blue at low saturation. The hue is the colour-vision-safe blue from
% the house palette; the tint keeps black text on it readable.
\definecolor{classblue}{HTML}{C7E0EE}
\definecolor{bandgrey}{HTML}{F4F4F4}

\begin{tikzpicture}[
  every node/.style = {font=\small},
  % --- coverage treatments ------------------------------------------------
  cov/.style     = {draw, thick, rectangle, rounded corners=1.5pt, fill=white,
                    align=center, inner sep=4pt, minimum height=8mm,
                    minimum width=1.9cm},
  modeled/.style = {cov, densely dashed, fill=classblue},
  uncov/.style   = {cov, densely dotted, fill=black!12, text=black!48},
  struct/.style  = {cov, very thick},
  % --- edges ---------------------------------------------------------------
  go/.style      = {->, >=stealth, thick},
  plain/.style   = {thick},
  lbl/.style     = {font=\scriptsize\itshape, align=center, inner sep=2pt},
  sub/.style     = {font=\small, inner sep=1pt},
  % --- the paradigm band ---------------------------------------------------
  % anchor=north so five tags of different heights hang from one line.
  % Hyphenation off: "Stochastic pro-gramming" across two lines is worse than
  % a short line.
  tag/.style     = {draw, rounded corners=4pt, thin, fill=white, anchor=north,
                    align=center, text width=4.05cm, inner sep=5pt,
                    font=\footnotesize, hyphens/.code={},
                    execute at begin node={\hyphenpenalty=10000\exhyphenpenalty=10000}},
]

% =========================================================================
% THE TREE.  Two symmetric vertical chains -- convex on the left, mixed-integer
% on the right -- so both inclusions read top-to-bottom, GENERAL to SPECIFIC,
% the same direction the tree above them narrows, and neither costs any width.
% =========================================================================
\node[struct] (root) at (0.29,0) {Optimization problem};

\node[struct] (cont) at (-3.33,-1.7) {Continuous variables\\ (NLP)};
\node[struct] (mip)  at (3.9,-1.7)   {Some discrete variables\\ (mixed-integer)};

\draw[go] (root) -- (cont);
\draw[go] (root) -- (mip);

% --- continuous branch ---------------------------------------------------
\node[cov]   (smooth)    at (-4.65,-3.4) {Differentiable\\ (smooth)};
\node[uncov] (nonsmooth) at (-1.0,-3.4)  {Nondifferentiable\\ (nonsmooth)};

\draw[go] (cont) -- (smooth);
\draw[go] (cont) -- (nonsmooth);

% CONVEX IS MIDDLE-STATE. Prof. Dowling, 2026-08-25, correcting his own first
% answer to NB14: "Also shade convexity with the 'specialized algorithms exist'
% color. I selected the wrong answer." Convex analysis IS covered -- KKT,
% constraint qualifications, Part II theory -- but convex SOLVERS are not, which
% is the same relationship LP and QP have to this course.
%
% ⚠ The chain below therefore shades middle / not-covered / not-covered /
% middle / middle downward. THAT DISCONTINUITY IS CORRECT and must not be
% smoothed out: the chain order is a mathematical inclusion and outranks
% visual tidiness.
\node[modeled] (convex) at (-6.0,-5.1) {Convex};
\node[cov] (nonconvex) at (-3.3,-5.1) {Nonconvex\\ NLP};

\draw[go] (smooth) -- (convex);
\draw[go] (smooth) -- (nonconvex);

% The convex classes as a CHAIN OF INCLUSIONS, which is the whole reason this
% figure differs from Biegler's: every SDP contains the SOCPs as the case where
% the matrices are block-diagonal; every SOCP contains the convex QPs (indeed
% the convex QCQPs), which contain the LPs as the case of a zero Hessian.
% Biegler's flat Convex -> {LP, QP} cannot say any of that.
%
% The edge Convex -> SDP is an IS-A edge like every other edge in the tree, so
% it claims only "an SDP is a convex program", which is true. It does NOT claim
% SDP is the most general convex program -- a general convex NLP is broader --
% and "Convex" remains the head of the branch for exactly that reason.
\node[uncov]   (sdp)  at (-6.0,-6.5)  {SDP};
\node[uncov]   (socp) at (-6.0,-7.7)  {SOCP};
\node[modeled] (qp)   at (-6.0,-8.9)  {QP};
\node[modeled] (lp)   at (-6.0,-10.1) {LP};

\draw[go] (convex) -- (sdp);
\draw[go] (sdp)  -- node[sub, right] {$\supseteq$} (socp);
\draw[go] (socp) -- node[sub, right] {$\supseteq$} (qp);
\draw[go] (qp)   -- node[sub, right] {$\supseteq$} (lp);

% BRACE over SDP + SOCP. Prof. Dowling, 2026-08-25: "Add the brace for SDP and
% SOCP. I like the 'conic' label. We do not need to specify that those are not
% covered. That is clear from the color coding." -- so the label names the
% class and nothing else, and the brace carries NO coverage information.
% (It now reads "conic optimization" on two horizontal lines; see below.) Coverage is already
% encoded four ways (dotted rule, grey fill, grey text, legend); a fifth would
% make the brace look like it meant something extra.
%
% Its real job is the SHADING DISCONTINUITY. Read down the chain and the
% shading goes middle / not-covered / not-covered / middle / middle, because
% the chain is ordered by inclusion and the shading by what we cover. The brace
% groups the two dotted boxes so the eye reads ONE interruption with a reason
% rather than an alternation that looks like a mistake.
%
% ⚠ GEOMETRY: this brace must not widen the figure. It lives entirely inside
% the gap between the chain's right edge (x = -5.05) and the annotation, and
% the "more specialized" label was moved DOWN, not out, to make room. Width is
% 1803 px before and after -- checked, because the site fixes the rendered
% width at 720 CSS px and any growth shrinks all the type.
%
% 🔴 DRAWN BY HAND, and NOT with decorations.pathreplacing's `brace'. That
% library is not in figures/preamble.tex, and loading it with a body-level
% \usetikzlibrary -- which is what this figure first did -- COMPILES CLEANLY
% AND SILENTLY WIDENS THE PICTURE BY 43 pt. Loading a library in the body
% emits stray horizontal material, and inside a `standalone' document that
% material is part of the page: 432.588 pt -> 475.759 pt, i.e. 1803 px -> 1983
% px, with no warning of any kind. A trailing % only reduced it to 472.438 pt.
% Measured with pdfinfo on both, after a smoke test that proved it compiled and
% proved nothing about the width.
%
% Adding the library to preamble.tex instead would work, but that file is
% shared by all eight diagrams AND \input by the lecture handouts, so it is not
% something to change to serve one brace. Two arcs and a nub need no library at
% all -- `to[out=,in=]' is core TikZ.
%
% Geometry: spine at x = -5.02 (the chain's boxes end at -5.05), nub 0.22 cm to
% its right, spanning SDP's top edge to SOCP's bottom edge.
\draw[thick]
  (-5.02,-6.10) to[out=0,in=90]   (-4.91,-6.24)
              -- (-4.91,-6.96) to[out=-90,in=180] (-4.80,-7.10)
              to[out=180,in=90]  (-4.91,-7.24)
              -- (-4.91,-7.96) to[out=-90,in=0]   (-5.02,-8.10);
% LABEL SET HORIZONTALLY, ON TWO LINES, IN THE PLAIN BODY FACE. Prof. Dowling,
% 2026-08-25. It was rotated to follow the brace and read "conic"; rotated type
% is harder to read at the size the website renders this figure, and "conic"
% alone under-names the class. It sits in the gap the deleted italic note
% (below) vacated, so the two-line horizontal form costs no width -- re-measured
% at 432.588 pt / 1803 px after the change, which is the number that matters.
\node[font=\footnotesize, anchor=west, align=center] at (-4.68,-7.10)
      {conic\\ optimization};

% OUTSIDE the chain, not inside it. Inside, it stacked directly under the
% brace's "conic" label in the same narrow column and the two read as one
% vertical string, "conic more specialized". Outside is also the symmetric
% placement: the mixed-integer chain carries the same label on ITS outer side.
% ⚠ x = -7.05 is deliberate -- the label extends LEFTWARDS from its anchor to
% about -7.37, and the band's left edge is -7.40. Any further left and it sets
% the bounding box and widens the figure. Page width re-measured after moving
% it: 432.588 pt, unchanged.
\node[lbl, anchor=north, rotate=-90] at (-7.05,-6.20) {more specialized $\longrightarrow$};

% DELETED 2026-08-25, on Prof. Dowling's instruction -- removed, not trimmed.
% An italic note sat here reading "generalize LP and QP --- this is where they
% fit" (itself already a trim of a longer sentence). It is gone entirely and
% nothing replaces it: the notebook prose still explains where conic
% optimization fits, so the idea survives on the page rather than in the
% picture. Its space is now the brace label's.

% --- mixed-integer branch ------------------------------------------------
% Same reading direction as the convex chain: MINLP >= MIQP >= MILP >= IP/BIP.
\node[cov]     (minlp) at (3.9,-3.4) {MINLP};
\node[modeled] (miqp)  at (3.9,-4.7) {MIQP};
\node[cov]     (milp)  at (3.9,-6.0) {MILP};
\node[cov]     (ip)    at (3.9,-7.45) {IP / BIP\\ \scriptsize all variables integer};

\draw[go] (mip)   -- (minlp);
\draw[go] (minlp) -- node[sub, right] {$\supseteq$} (miqp);
\draw[go] (miqp)  -- node[sub, right] {$\supseteq$} (milp);
\draw[go] (milp)  -- node[sub, right] {$\supseteq$} (ip);

\node[lbl, anchor=north, rotate=-90] at (5.7,-4.1) {more specialized $\longrightarrow$};

% =========================================================================
% LEGEND -- placed in the dead space under the mixed-integer chain rather
% than in a strip of its own, which would cost 2 cm of width-scaled height.
% It repeats all three treatments, so a black-and-white reader can decode the
% figure from here without seeing a single hue.
%
% Wording is Prof. Dowling's, 2026-08-25. The middle label is set on TWO LINES
% rather than one: the figure's width is what fixes its on-screen type size,
% so a longer label must grow downward, never sideways.
% =========================================================================
\node[cov, minimum width=7mm, minimum height=4.5mm, inner sep=1pt] at (0.9,-8.75) {};
\node[anchor=west, font=\footnotesize] at (1.35,-8.75)
      {\textbf{Our focus this semester}};

\node[modeled, minimum width=7mm, minimum height=4.5mm, inner sep=1pt] at (0.9,-9.7) {};
\node[anchor=west, font=\footnotesize, align=left] at (1.35,-9.7)
      {\textbf{Specialized algorithms}\\ \textbf{not covered this semester}};

\node[uncov, minimum width=7mm, minimum height=4.5mm, inner sep=1pt] at (0.9,-10.6) {};
\node[anchor=west, font=\footnotesize] at (1.35,-10.6)
      {\textbf{Not covered this semester}};

% =========================================================================
% THE PARADIGM BAND.  Drawn as a different KIND of object -- rounded tags in a
% dashed enclosure, no tree edges, and ONE arrow that points at the tree as a
% whole rather than at any node in it. That is the orthogonality claim, drawn.
%
% CUT BACK TO A LIST 2026-08-25, on Prof. Dowling's instruction: "Let's cut a
% lot of the text in the modeling paradigms box. Instead, let's just list the
% paradigms." The per-tag glosses ("discretizes into a large NLP", "an LP or an
% MINLP, only bigger") and the explanatory paragraph MOVED INTO THE NOTEBOOK
% PROSE rather than being deleted -- orthogonality is the point students most
% often miss and is the whole reason the band exists. Exactly ONE line of it
% stays here, because a band of five bare names claims nothing at all.
% =========================================================================
\fill[bandgrey, rounded corners=6pt] (-7.4,-14.95) rectangle (7.2,-11.35);
\draw[dashed, thick, rounded corners=6pt, black!55]
      (-7.4,-14.95) rectangle (7.2,-11.35);

\draw[->, >=stealth, very thick, dashed, black!55] (-1.0,-11.35) -- (-1.0,-10.75);

\node[anchor=north west, align=left, font=\small] at (-7.15,-11.50)
      {\textbf{Modeling paradigms}\\
       \footnotesize These cut \emph{across} the tree above: each is realized \textbf{as} one of the classes
       above, not as another branch of it.};

\node[tag] (par1) at (-4.93,-12.75) {\textbf{Dynamic optimization}};
\node[tag] (par2) at (-0.11,-12.75) {\textbf{Parameter estimation and experimental design}};
\node[tag] (par3) at (4.72,-12.75)  {\textbf{Stochastic programming}};
\node[tag] (par4) at (-4.93,-14.05) {\textbf{Logical models (GDP)}};
\node[tag] (par5) at (-0.11,-14.05) {\textbf{Decision support}};

\end{tikzpicture}
